Refine ChatDoctor dataset preprocessing with dynamic diagnostic question variations

- Updated `preprocess_chatdoctor_dataset.py` to eliminate repetitive static prefix strings ("Before recommending a self-care plan").
- Implemented 60+ dynamic, natural conversational question variations and transitions to prevent LLM fine-tuning memorization and token artifact overfitting.
- Strengthened exclusion filters to catch hyphenated surgical and inpatient procedure terms (e.g. `by-pass`, `post-op`).
- Exported 750 clean ChatML pairs to `datasets/fine-tuning/chatdoctor-clean.json` (avg 90.9 words per assistant turn).
- Updated `dataset_preprocessing_report.md` artifact with refined methodology details.
