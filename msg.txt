feat(cache): implement 2-tier caching across high-impact services

- Expand CacheService to support generic modular get/set with JSON object serialization and fallback to in-memory TTLCache.
- Integrate 2-tier Valkey caching into TranslationService (trans: key namespace, 7-day TTL).
- Replace process-bound TTLCache in OlaMapsService with 2-tier cache (geo: key namespace, 2-hour TTL).
- Integrate 2-tier caching into HealthCareSchemesService RAG search (rag: key namespace, 24-hour TTL).
- Integrate 2-tier caching into UserIntentClassifierService (intent: key namespace, 7-day TTL).
- Add cache TTL settings in app/core/config.py.
